[project]
name = "corrosion-haks"
version = "0.1.0"
description = "HAKS Airbus x IBM x AWS 2026 hackathon - corrosion risk prediction"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
